import React from 'react'

function Task1() {
  return (
    <div>
        <h6>Task1</h6>
        <h2>List Rendering Introduction:</h2>
        <p>React Lists are used to display a collection of similar data items like an array of objects and menu items. It allows us to dynamically render the array elements and display repetitive data.</p>
    <h3>Rendering List in React</h3>
    <p>To render a list in React, we will use the JavaScript array map function. We will iterate the array using map() and return the required element in the form of JSX to render the repetitive elements.</p>
    </div>
  )
}

export default Task1